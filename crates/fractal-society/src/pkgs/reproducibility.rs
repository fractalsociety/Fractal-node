//! Work-package stub (architect-owned). **Package 4 — reproducibility.**
//!
//! Re-run an adapter+agent from a `RunManifest` via `kernel::replay` and confirm
//! the reproduced `evidence_hash` matches the recorded one. Returns a
//! `VerifierReport`.
//!
//! See `crates/fractal-society/WORK_PACKAGES.md` for the exact interface,
//! acceptance tests, and constraints. Replace this stub with the implementation.
//! Edit ONLY this file and your test file `tests/wp_reproducibility.rs`.
