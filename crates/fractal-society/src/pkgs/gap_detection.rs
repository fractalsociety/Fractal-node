//! Work-package stub (architect-owned). **Package 25 — gap_detection.**
//!
//! Detect gaps in an ordered integer sequence (e.g. bar timestamps / sequence
//! numbers) and emit explicit `DataGap` records (never silently interpolate).
//!
//! See `crates/fractal-society/WORK_PACKAGES.md` (second batch) for the exact
//! interface, acceptance tests, and constraints. Replace this stub with the
//! implementation. Edit ONLY this file and your test file
//! `tests/wp_gap_detection.rs`.
