//! Work-package stub (architect-owned). **Package 30 — pipeline_contract.**
//!
//! Interface-first data model for the future end-to-end orchestrator:
//! `PipelineStage`, `PipelineOutcome`, and a well-formedness `validate`. This
//! package defines the spine's TYPES only (no wiring) so the orchestrator task
//! can compose them later without new design.
//!
//! See `crates/fractal-society/WORK_PACKAGES.md` (third batch) for the exact
//! interface, acceptance tests, and constraints. Replace this stub. Edit ONLY
//! this file and your test file `tests/wp_pipeline_contract.rs`.
