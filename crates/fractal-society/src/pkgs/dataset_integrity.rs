//! Work-package stub (architect-owned). **Package 5 — dataset_integrity.**
//!
//! Validate `DatasetManifest` required fields and confirm `content_hash`
//! stability (identical content → identical hash; one-byte change → different
//! hash). Returns a `VerifierReport`.
//!
//! See `crates/fractal-society/WORK_PACKAGES.md` for the exact interface,
//! acceptance tests, and constraints. Replace this stub with the implementation.
//! Edit ONLY this file and your test file `tests/wp_dataset_integrity.rs`.
