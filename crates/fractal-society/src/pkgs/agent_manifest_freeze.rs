//! Work-package stub (architect-owned). **Package 12 — agent_manifest_freeze.**
//!
//! Build a frozen `AgentManifest` from agent metadata (code bytes, dependency
//! ids/versions, tool allowlist), computing canonical content hashes so the
//! manifest is tamper-evident at submission time.
//!
//! See `crates/fractal-society/WORK_PACKAGES.md` for the exact interface,
//! acceptance tests, and constraints. Replace this stub with the implementation.
//! Edit ONLY this file and your test file `tests/wp_agent_manifest_freeze.rs`.
